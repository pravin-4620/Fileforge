import { outputPath,run,assertSupported } from './base.js'
const video=['mp4','avi','mov','mkv','flv','webm','gif'];const audio=['mp3','wav','aac','flac','ogg','m4a']
export default {category:['video','audio'],supported:[...video,...audio],canConvert:(from,to)=>[...video,...audio].includes(from)&&[...video,...audio].includes(to),async convert(input,target){assertSupported(target,[...video,...audio],'Media converter');const out=outputPath(input,target);const threads=String(Math.max(1,Number(process.env.FFMPEG_THREADS||1)));const args=['-y','-i',input];if(target==='gif')args.push('-vf','fps=12,scale=720:-1:flags=lanczos','-loop','0');else if(audio.includes(target))args.push('-vn');else if(target==='mp4')args.push('-c:v','libx264','-preset','medium','-crf','23','-c:a','aac','-movflags','+faststart');args.push('-threads',threads,out);await run(process.env.FFMPEG_PATH||'ffmpeg',args);return out}}
